# -*- coding: utf-8 -*-

"""
Public API for the ``config`` subpackage.

Import ``Config`` and ``LbdFunc`` from here instead of reaching into the
numbered internal modules.
"""

from .config_00_main import Config
from .config_01_lbd_func import LbdFunc
